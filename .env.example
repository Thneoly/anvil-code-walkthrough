# 本地开发时可将 BASE_URL 覆盖为根路径，避免静态资源路径受 GitHub Pages baseUrl 影响
# 复制本文件为 .env 然后按需修改
# macOS/zsh 下也可直接在命令行前缀导出，而无需 .env 文件
# 示例： BASE_URL=/ npm run start

# 将站点基础路径设为根，用于本地开发
BASE_URL=/

# 可选：Algolia DocSearch（开源项目可免费申请 https://docsearch.algolia.com/ ）
# 配置后将自动在站点启用搜索（未配置则不启用）
DOCSEARCH_APP_ID=
DOCSEARCH_API_KEY=
DOCSEARCH_INDEX_NAME=
